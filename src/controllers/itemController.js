const itemController = (() => {
  const itemGet = (req, res) => { };
  const itemEditGet = (req, res) => { };
  const itemEditPost = [null, (req, res) => { }];

  return { itemGet, itemEditGet, itemEditPost };
})();

export default itemController;
